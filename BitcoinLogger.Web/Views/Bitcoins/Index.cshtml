@model List<BitcoinLogger.Entites.Bitcoin>

<h1>Listing With all bitcoin price entries</h1>
<hr />


<h1>@Model.Count()</h1>







<div class="d-flex justify-content-between py-3">
    <h2>Bitcoin Price</h2>
    <div class="currency_option">
        <label for="">Select your Currency</label>
        <select id="currency">
            <option value="usd">USD</option>
            <option value="euro">EUR</option>
        </select>
    </div>
</div>

@foreach (var item in Model)
{
    @item.Source
    <br />
    @item.Price
    <br />
    @item.Date.ToString()
}



@*<table id="BitcoinPricelistTable" class="display">

    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Source)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th></th>
        </tr>
    </thead>


    <tbody>
        @foreach (var item in Model)
        {

            <tr>
                <td>@item.Source</td>
                <td>
                    <span class="price"> @Html.DisplayFor(modelItem => item.Price) </span> <span class="c_sign"></span>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    <a href="#">View more &rarr;</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@

